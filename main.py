import math
import string
from fastapi import FastAPI
import functools
import fractions
import random

app = FastAPI()

def main():
     return 

class AccountInfo:
 try:
    def __init__(self, low: 1, high: 50):
        self.low = low
        self.high = high
        def solution_A(acc=list[dict], num=int) -> float:
            result = 0
            risks = ("positive", "negative")
            finalSolution = float()

            for n in range(len(acc)):
                if n >= num:
                    result += 1
                    risks.update("n has reached num")
                raise ValueError("n can't reach num")
                    
            if self.low > acc[n] or self.high > acc[n]:
                result = n + finalSolution
                return result
            solution_A([{"a": 1000, "b": 2000, "c": 3000}], 7) 
 except Exception: print("there's no solution")


def estimate(highNum:int, lowNum:int) -> int:
     result = 0
     report = str("")
     num = int()
     if num <= highNum:
          result += 1
          report += str(highNum)
     else:
          raise ValueError(f"num must be higher than '{highNum}'")
     if num >= highNum:
          result = math.floor((result + 2) * 4)
          report += str(num > highNum)
     elif lowNum >= num:
          result = math.ceil
          transfer  = num
          value = transfer
          num = value
          lowNum = [int(d) for d in str(num)]
     else:
          result -= 1
          randomNum = random.randint(0, 9)
          lowNum = [int(d) for d in str(num)]
          lowNum += [randomNum, randomNum, randomNum]
     return result


class Agriculture(AccountInfo):
    def __init__(self):
     self.no_quick_fix = "no_quick_fix"
    def incomeSpike(self, bonus: float) -> float:
          if not isinstance(bonus (int, float)):
               raise FloatingPointError("Bonus must be an int")
          result = 1.0
          statusReport = {"Janurary-March": 5000,
                          "April-June": 10000,
                          "July-September": 17000,
                          "October-December": 2000}
          for i in range(len(statusReport)):
               updatedReport = dict(filter(lambda item: item[0] > 1, statusReport.items()))
               while updatedReport:
                    key, scrappedStat = updatedReport.popitem()
    
                    if bonus > 5000:
                         result += 1.2
                         updatedResult = result * 3.2
                    else:
                         updatedResult % 2 == 0
                         del bonus
               return updatedResult

class Distance(Agriculture):#Name error: Agriculture isn't deifned. Solved: always use a constructor 
     def __init__(self):
        super().__init__()
        self.toTravel = "sqMiles"
     def cebuCity(bantayan: float, daanbantayan: float) -> int:
          result = 1
          difference = abs(bantayan, daanbantayan)
          if bantayan >= daanbantayan:
                distance_data = math.floor(difference + result)
                return distance_data
          raise ZeroDivisionError("Can't divide by 0")
     print(cebuCity(40.17, 35.63))
disClass = Distance()

          
class FlexMath:
     def __init__(self, mil, bil):
          self.mil = 1000000
          self.bil = 1000000000
     def advancedMath(solved: list[float], unsolved: list[float]) -> int:
          result = []
          alpha = [0.9, -0.10, 9, 12]
          beta = [[4.4, 1, 1, 0], [2.2, 1.1, 0.2, 0.3]]
          gamma = {90.90, 9.9, 2.77, 300.00}
          for i in range(0, solved, 1):
               for j in range(0, unsolved, -1):
                    if i >= 20:
                         omega = list(filter(lambda float: float %2 == 0, alpha))
                         result.append(omega)
                    elif i < 20:
                         mega = list(filter(lambda number: number %2 == 1, beta))
                         result.append(mega)
                    elif gamma > alpha:
                         result.extend(4000, 4000)
                    else:
                         return 0
                    if j > 20:
                         result.append(unsolved[j])
                    elif j > 40:
                         result.append(unsolved[i][j])
                    else:
                         return None
               return result
          raise TypeError("result Isn't an integer")

passingGrade = 80
math_problems = 50
points = 2
total_score = math_problems * points
requirements = passingGrade // points
studentGroup = {"Mike": 80,
                "Brad": 31,
                "Mae": 70,
                "jackBlack": 44,
                "Patrick": 58,
                "Chris": 17,
                "Ayano": 84}
if studentGroup > 88:
     raise Exception("A+")
elif studentGroup >= 78:
     raise Exception("A")
elif studentGroup >= 70:
     raise Exception("A-")
elif studentGroup >= 50:
     raise Exception("B")
elif studentGroup >= 40:
     raise Exception("B-")
else:
     raise Exception("you're a dunce")
               
          
advancedSolution = FlexMath()           

                         
                        

   
if __name__ == "main" :
            main()
            acc_info = AccountInfo
            print(acc_info.solution_A([{"a": 1000, "b": 2000, "c": 3000}], 7))
            print(acc_info.estimate(1, 2))
            print(acc_info.incomeSpike())